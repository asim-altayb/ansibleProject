---
apache_service: apache24
apache_daemon: apache24
apache_daemon_path: /usr/sbin/
apache_server_root: /etc/apache24
apache_conf_path: /etc/apache24

__apache_packages:
  - apache24

apache_ports_configuration_items:
  - regexp: "^Listen "
    line: "Listen {{ apache_listen_port }}"